import { resolve } from 'path';

export const lyricsPath = resolve(__dirname, '../../../static/lyrics');

export const root = resolve(__dirname, '../../../');

export const baseUrl = 'http://localhost:3000/api';
