import { resolve } from 'path';

export const rootPath = resolve(__dirname, '../../../../wakmusic/');
export const lyricsPath = resolve(rootPath, 'src/lyrics');

export const staticPath = resolve(rootPath, 'src');
export const staticNewsPath = resolve(rootPath, 'src/images/news');
export const staticArtistPath = resolve(rootPath, 'src/images/artist');
export const staticProfilePath = resolve(rootPath, 'src/images/profile');
export const staticPlaylistPath = resolve(rootPath, 'src/images/playlist');
